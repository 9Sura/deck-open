.DS_Store
__pycache__/
*.pyc

# Secrets. Never commit a real key.
.env
.env.*
!.env.local.example

# Derived from the committed question bank by `npm run derive-bank`, not
# committed. bank-manifest.json is a count index and is harmless; pi-inventory
# is a standalone map of the DECA performance-indicator taxonomy, which this
# project deliberately does not publish as a liftable file. Generate it locally.
frontend/lib/data/pi-inventory.json

# Node / Next.
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.vercel

# Python
venv/
.venv/
